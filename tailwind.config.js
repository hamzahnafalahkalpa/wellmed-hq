/** @type {import('tailwindcss').Config} */
export default {
    darkMode: 'media', // ⬅️ penting
    important: false,
    theme: {
        extend: {
            colors: {         
            }
        },
    }
};
